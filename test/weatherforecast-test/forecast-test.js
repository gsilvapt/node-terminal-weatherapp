/**
 * Tests to ensure the forecast functionality of the app is working as expected.
 */

// Assert if key does not exist, it gets an empty object.

// Assert if key exists, can get a json object with data